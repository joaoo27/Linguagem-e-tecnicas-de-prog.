#include <stdio.h>
#include <stdlib.h>
int main() {
	
	
	float area, base, altura;
	
      base = 10;
      altura = 20;
      area = (base*altura)/2;
	
	printf("A area do triangulo (%.2f) * (%.2f) = %.2f", base,altura,area);
	
	
	return 0;
}
